const initialState = {
    categories: [
        // categorías aquí
    ],
    products: [
        // productos aquí
    ],
    orders: [
        // órdenes aquí
    ]
};

export default initialState;
